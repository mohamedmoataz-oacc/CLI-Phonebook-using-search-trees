An in-memory phone book that loads contacts from a csv file, inserts them into a variety of tree structures (AVL, Two-Four Tree, Heap, Trie) and can be used to update contacts or search for contacts.
